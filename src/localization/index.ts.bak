/**
 * Localization Module
 * 
 * Provides multi-language support for the amazonq-sdd package
 */

export { LocalizationManager, LocalizationOptions } from './LocalizationManager';
export { 
  LanguageDetectionService, 
  LanguageDetectionOptions, 
  LanguageDetector 
} from './LanguageDetector';

// Re-export types for convenience
export { Language, LocalizedStrings } from '../types';